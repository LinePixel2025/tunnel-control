# Tunnel Control 部署流程

## 适用范围

本版本使用 HTTP/WS 单端口模式，不需要备案域名、HTTPS 证书或 443/8443 端口。管理登录、设备令牌及隧道数据会以明文传输，请仅在受信网络、VPN，或严格限制来源 IP 的安全组中使用。

系统结构如下：

```text
浏览器 / Windows Agent
        |
        | http:// 或 ws://<公网IP>:<CONTROL_PORT>
        v
admin (Nginx: 管理网页、API、WebSocket 反向代理)
        |
        v
server (隧道控制服务，Docker 内部 18080)

公网业务端口 <TUNNEL_PORT_START>-<TUNNEL_PORT_END>
        |
        v
server -> Windows Agent -> Windows 本地服务
```

`CONTROL_PORT` 仅决定公网管理页面和控制通道的端口。服务内部固定使用 `18080`，因此修改 `CONTROL_PORT` 不需要改代码。`TUNNEL_PORT_START` 和 `TUNNEL_PORT_END` 是实际可分配的公网隧道范围。

## 1. 准备服务器

需要 Linux 公网服务器、Docker Engine 和 Docker Compose plugin。不要将 PostgreSQL `5432` 或 Redis `6379` 暴露到公网。

在腾讯云安全组和 Ubuntu 防火墙中开放：

| 用途 | 协议/端口 | 建议来源 |
| --- | --- | --- |
| 管理台与 Agent 控制通道 | TCP `CONTROL_PORT`，例如 `18080` | 管理员与 Windows 设备的可信 IP |
| 公网隧道 | TCP `TUNNEL_PORT_START-TUNNEL_PORT_END`，例如 `10000-10100` | 按业务实际需要限制 |

Ubuntu 使用 UFW 时，例如：

```bash
sudo ufw allow 18080/tcp
sudo ufw allow 10000:10100/tcp
sudo ufw status numbered
```

## 2. 准备配置

在项目根目录执行：

```bash
cp deploy/.env.example deploy/.env
openssl rand -hex 32
```

编辑 `deploy/.env`，将所有示例密钥替换为随机长字符串：

```dotenv
CONTROL_PORT=18080
TUNNEL_PORT_START=10000
TUNNEL_PORT_END=10100
POSTGRES_PASSWORD=<随机数据库密码>
JWT_SECRET=<随机 JWT 密钥>
BOOTSTRAP_AGENT_TOKEN=<随机初始设备令牌>
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=<首次管理员强密码>
```

端口可以修改。例如服务器无法使用 `8080` 或 `18080` 时，设为未被占用的 `CONTROL_PORT=18081`，并同步修改安全组、UFW 和 Windows Agent 地址。

## 3. 启动服务

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml up -d --build
docker compose --env-file deploy/.env -f deploy/compose.yaml ps
curl -i http://127.0.0.1:${CONTROL_PORT}/healthz
```

预期健康检查返回 `HTTP/1.1 200 OK` 和 `ok`。排错使用：

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml logs -f server admin
```

浏览器访问 `http://<公网IP>:<CONTROL_PORT>`，使用 `BOOTSTRAP_ADMIN_EMAIL` 与 `BOOTSTRAP_ADMIN_PASSWORD` 登录。首次成功启动后，删除 `.env` 中的 `BOOTSTRAP_ADMIN_PASSWORD` 再执行一次 `docker compose ... up -d`，避免初始密码长期保留在配置文件。

## 4. 添加 Windows Agent

Agent 控制地址必须是：

```text
ws://<公网IP>:<CONTROL_PORT>/control
```

例如：

```text
ws://123.207.8.77:18080/control
```

以管理员身份运行 CLI 安装（不再需要任何令牌）：

```powershell
release\tunnel-agent.exe --install --server ws://123.207.8.77:18080/control
Get-Service TunnelAgent
release\tunnel-agent.exe logs
```

首次启动时代理进入设备码注册模式：`tunnel-agent.exe logs` 中会显示一次性 8 位注册码。在管理面板「设备注册」页输入该注册码批准，服务器生成访问令牌并直接下发给设备；之后设备列表显示“在线”，才能给它创建隧道。令牌保存在设备端 `%PROGRAMDATA%\TunnelControl\credentials`，用户全程不接触。

如不使用 Windows 服务，也可把 `tunnel-client.cmd` 与 `tunnel-agent.exe` 放在同一目录，双击 `tunnel-client.cmd` 进入一键脚本模式（自动启动代理，命令行输入 `stop` / `restart` / `status` / `logs` 控制，`exit` 退出但代理保持后台运行）。

需要让设备重新注册时，运行 `tunnel-agent.exe reset`（或脚本模式下输入 `reset`）：会停止正在运行的代理，删除本地的访问令牌、注册码、引导配置和日志；下次启动会重新打印注册码走设备码注册。若代理安装在 Program Files 下，请以管理员身份执行。

## 5. 创建并验证隧道

在管理面板新建隧道，公网端口必须落在 `.env` 的 `TUNNEL_PORT_START` 到 `TUNNEL_PORT_END` 之间。例如将 Windows 的本地 HTTP 服务 `127.0.0.1:3000` 映射为公网 `10080`。

验证 HTTP：

```bash
curl -v http://<公网IP>:10080/
```

验证任意 TCP 端口：

```bash
nc -vz <公网IP> 10080
```

服务端容器必须发布整个端口区间，因此每次修改 `TUNNEL_PORT_START` 或 `TUNNEL_PORT_END` 后都要执行：

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml up -d
```

## 6. 备份与恢复

备份 PostgreSQL：

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml exec -T postgres pg_dump -U tunnel tunnel > tunnel-$(date +%F).sql
```

恢复：

```bash
cat tunnel-YYYY-MM-DD.sql | docker compose --env-file deploy/.env -f deploy/compose.yaml exec -T postgres psql -U tunnel -d tunnel
```

Redis 只保存在线状态缓存，可在恢复时丢弃。请定期轮换管理员密码、JWT 密钥、数据库密码和设备令牌。

## 7. 自动部署（可选）

已提供 GitHub Actions 自动部署：推送 `main` 后自动同步代码到服务器并重建容器，且不会删除数据卷或服务器上的 `.env`。配置步骤见 [deploy/ci-deploy.md](deploy/ci-deploy.md)。
