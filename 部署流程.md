# Tunnel Control 部署流程（HTTP/WS 单端口）

## 重要安全说明

本部署不使用 HTTPS/WSS，也不需要备案或 443/8443 HTTPS 端口。管理 API、管理页面和 Windows 代理控制信道统一使用一个可设置端口：`http://<公网 IP>:<CONTROL_PORT>` 与 `ws://<公网 IP>:<CONTROL_PORT>/control`。示例使用 `18080`。

这意味着管理员密码、JWT、设备令牌和隧道数据都会以明文通过公网传输。仅应在以下任一条件成立时使用：服务器仅能从可信内网/VPN 访问、云安全组为管理端口设置固定 IP 白名单，或额外通过安全网络覆盖层保护流量。请不要把该端口对全网公开后用于生产敏感业务。

## 1. 服务器准备

准备一台有公网 IPv4 的 Linux 主机，推荐 Ubuntu 22.04+、2 vCPU、2 GB 内存。安装 Docker Engine 和 Docker Compose Plugin。

在云安全组和系统防火墙中开放：

| 用途 | TCP 端口 | 建议来源 |
| --- | --- | --- |
| 管理页面与控制信道 | `CONTROL_PORT`，默认 `18080` | 管理员和 Windows 客户端可信 IP |
| 公网隧道 | 实际分配的 `10000-60000` 端口 | 业务所需来源 |

不要开放 PostgreSQL `5432` 或 Redis `6379`。

## 2. 部署服务端

```bash
git clone <repository-url> tunnel-control
cd tunnel-control
cp .env.example deploy/.env
```

编辑 `deploy/.env`。使用 `openssl rand -hex 32` 生成 `POSTGRES_PASSWORD`、`JWT_SECRET` 和后续设备令牌。

```dotenv
POSTGRES_PASSWORD=<random-secret>
JWT_SECRET=<random-secret>
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=<strong-first-login-password>
CONTROL_PORT=18080
```

启动并检查：

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml up -d --build
docker compose --env-file deploy/.env -f deploy/compose.yaml ps
curl -fsS http://127.0.0.1:${CONTROL_PORT}/healthz
```

使用浏览器打开 `http://<server-ip>:18080`，用首次管理员账号登录。请替换为你在 `CONTROL_PORT` 中设置的实际端口。首次成功启动后，删除 `deploy/.env` 中的 `BOOTSTRAP_ADMIN_PASSWORD` 并再次执行：

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml up -d
```

## 3. 创建设备令牌

每台 Windows 设备使用独立令牌，数据库只保存 SHA-256 哈希。

```bash
TOKEN="$(openssl rand -hex 32)"
TOKEN_HASH="$(printf %s "$TOKEN" | sha256sum | awk '{print $1}')"
DEVICE_ID="$(uuidgen | tr '[:upper:]' '[:lower:]')"
docker compose --env-file deploy/.env -f deploy/compose.yaml exec -T postgres \
  psql -U tunnel -d tunnel -c "INSERT INTO devices(id,name) VALUES('$DEVICE_ID','WIN-DEV-01'); INSERT INTO access_tokens(id,device_id,label,token_hash) VALUES(gen_random_uuid(),'$DEVICE_ID','initial','$TOKEN_HASH');"
```

将输出的 `TOKEN` 仅通过受控密码管理器交给该设备的管理员。吊销令牌：

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml exec -T postgres \
  psql -U tunnel -d tunnel -c "UPDATE access_tokens SET revoked_at=now() WHERE device_id='$DEVICE_ID';"
```

## 4. 构建一键安装 EXE

在 Windows 构建机安装 Rust、Node.js 24+ 和 [Inno Setup](https://jrsoftware.org/isinfo.php)。打开 PowerShell：

```powershell
git clone <repository-url> tunnel-control
cd tunnel-control
cargo build --release -p tunnel-agent
.\deploy\windows\build-installer.ps1
```

产物为仓库根目录的 `release\Tunnel-Agent-Setup.exe`。这是原生 Rust EXE，不依赖 IExpress；它会在运行时复制自身为后台代理、注册 `TunnelAgent` 自动启动服务并配置失败重试。

安装前需要在目标机器上设置服务连接信息。以管理员 PowerShell 执行：

```powershell
Start-Process -FilePath .\release\Tunnel-Agent-Setup.exe -Verb RunAs -Wait
Get-Service TunnelAgent
```

安装器会交互要求输入 `ws://<server-ip>:<CONTROL_PORT>/control` 和设备令牌，并将其存放于 `C:\Program Files\TunnelControl\agent.env`。卸载时使用 `deploy\windows\uninstall-service.ps1` 删除 `TunnelAgent` 服务。更高安全要求时应改为 DPAPI 或 Windows Credential Manager 保存。

## 5. 创建隧道与验证

管理员为在线设备创建 TCP 或 HTTP 隧道，公网端口范围为 `10000-60000`。HTTP 是原样明文 TCP 转发，适用于 `http://<server-ip>:<port>`；TCP 可用于远程桌面、SSH 与数据库。

每条公网连接都通过单条 WebSocket 控制信道独立复用：`StreamOpen`、二进制 `StreamData`、`StreamClose`。验证：

```bash
curl -v http://<server-ip>:10080/
nc -vz <server-ip> 10001
docker compose --env-file deploy/.env -f deploy/compose.yaml logs -f server
```

## 6. 备份、升级与恢复

```bash
docker compose --env-file deploy/.env -f deploy/compose.yaml exec -T postgres pg_dump -U tunnel tunnel > tunnel-$(date +%F).sql
docker compose --env-file deploy/.env -f deploy/compose.yaml up -d --build
```

服务端启动时自动执行 SQL migration。恢复：

```bash
cat tunnel-YYYY-MM-DD.sql | docker compose --env-file deploy/.env -f deploy/compose.yaml exec -T postgres psql -U tunnel -d tunnel
```

Redis 只保存在线会话状态，可丢弃。定期轮换管理员密码、JWT 密钥、数据库密码和设备令牌，并将管理端口限制为可信来源。
